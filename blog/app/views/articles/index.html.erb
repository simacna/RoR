<h1> Listing articles </h1>
<%= link_to 'My Blog', controller: 'articles' %>

<%= link_to 'New article', new_article_path %> <!-- How does it know where to go -->
<table>
	<tr>
		<th> Title </th>
		<th> Text </th>
	</tr>

	<% @articles.each do |article| %> <!-- Why is this seen in the locaholhost:3000/articles and not /articles/index -->
	<tr>
		<td><%= article.title %></td>
		<td><%= article.text %></td>
	</tr>
	<% end %>
</table>